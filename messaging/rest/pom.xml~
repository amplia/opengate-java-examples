<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<!--
	###############################################
	## Pom Information 
	###############################################
	-->
	<artifactId>opengate-mfs-client-examples-messaging-rest</artifactId>
	<packaging>jar</packaging>
	<name>OpenGate MFS REST Messaging Client Examples</name>
	<parent>
		<groupId>es.amplia.opengate.mfs</groupId>
		<artifactId>opengate-mfs-client-examples-messaging</artifactId>
		<version>og5-SNAPSHOT</version>
	</parent>
	<build>
		<defaultGoal>package</defaultGoal>	
	
		<plugins>
		<plugin>
			<artifactId>maven-source-plugin</artifactId>
				<configuration>
					<attach>true</attach>
				</configuration>
			
				<executions>
				<execution>
					<phase>package</phase>
					<goals>
						<goal>jar</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
		
		<plugin>
			<artifactId>maven-jar-plugin</artifactId>
			<configuration>
					<archive>
						<manifestFile>target/buildinfo-manifest.mf</manifestFile>
					</archive>
				</configuration>
			
		<executions>
			
		
			<execution>
				<id>doc</id>
				<goals>
					<goal>jar</goal>
				</goals>
				<configuration>
					<classifier>doc</classifier>
					<classesDirectory>${project.build.directory}/../doc</classesDirectory>
					<useDefaultManifestFile>false</useDefaultManifestFile>
					<includes>
						<include>
							**/*
						</include>
					</includes>
					<excludes>
						<exclude>CVS</exclude>
						<exclude>CVS/*</exclude>
					</excludes>
				</configuration>
			</execution>
		
	
		</executions>
	</plugin>
		</plugins>
	</build>
	<!--
	###############################################
	## Dependencies
	###############################################
	-->	
	<dependencies>		
  </dependencies>
	<!-- 
	###############################################
	## Maven environment
	###############################################
	-->
	
	
		
		
		<distributionManagement>
			<repository>
				<id>amplia-repository</id>
				<name>Amplia Release Repository</name>
				<url>http://${amplia.repository.home}/amplia-releases</url>
			</repository>
			<snapshotRepository>
				<id>amplia-repository</id>
				<name>Amplia Snapshot Repository</name>
				<url>http://${amplia.repository.home}/amplia-snapshots</url>
			</snapshotRepository>
		</distributionManagement>
	
	
</project>